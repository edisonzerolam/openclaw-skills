# 审计报告格式

> 版本：v1.0 | 状态：active
> 来源：auditor v6.2 S5 报告输出模板

---

## 标准审计报告模板

```markdown
# {审计对象} 审计记录 #{seq}

**时间**：{YYYY-MM-DD HH:MM}
**场景**：{审计背景}
**方法**：v6.2 流程（S1→S2→S4→S3→S5）

---

## S1 — 战略评估

| 项目 | 内容 |
|------|------|
| 审计对象 | {对象} |
| 风险等级 | L1/L2(豁免?)/L2(强制S4)/L3/L4 |
| 分类 | {G1分类结果} |
| E5备份 | 需要/不适用 |
| E5备份tag | {audit-backup-YYYYMMDD-0001} |
| 子代理次数 | N次 |
| S4豁免判定 | L1/L2跳过S4：满足/不满足 |
| 增强层使用 | A~L中启用的层 |

---

## S2 — 规划合并（子代理输出）

```json
{S2子代理返回的JSON}
```

---

## S4 — 备份+验证（L1/L2豁免时填"跳过"）

**E5备份记录**（L3+必填）：

| 项目 | 内容 |
|------|------|
| 备份tag | {audit-backup-YYYYMMDD-0001} |
| 备份文件 | {被备份的文件路径} |
| 备份时间 | {ISO时间} |

**S4验证结果**：

```json
{S4子代理返回的JSON，8项验证项}
```

**S4.1 内容层检查记录**：

| 检查项 | 状态 | 发现 |
|--------|------|------|
| 阈值一致性 | pass/fail/不适用 | {列出} |
| 数据新鲜度 | pass/fail | {列出超过24小时的陈旧数据项} |
| 跨文件引用 | pass/fail | {列出引用路径错误项} |

---

## S3 — Merge Gate

| 检查项 | 结果 |
|--------|------|
| S2 schema合规 | pass/fail |
| S4验证结果 | pass/fail/跳过/不可用 |
| S4豁免后强制调用 | 是/否 |
| fail项分类 | 核心/非核心/无 |

**判定**：✅/⚠️/❌ {结论}

---

## 行为红线违规记录（Q6检查时）

> Q6 全 pass 时，此节可省略。

| # | 规则 | 违规判定 | 证据/来源 | 处理结果 |
|---|------|---------|---------|---------|
| — | — | — | — | — |

---

## S5 — 结果沉淀

### 发现的问题及修复

| # | 问题 | 层级 | 优先级 | 修复措施 |
|---|------|------|--------|---------|
| 1 | ... | 配置/内容 | P0/P1/P2 | ... |

### 增强层使用统计

| 增强层 | 使用 | 连续clean |
|--------|------|-----------|
| A | 是/否/降级 | n |
| B | 是/否/降级 | n |
| ... | ... | ... |

### Token消耗

| 步骤 | 方式 | 估算token |
|------|------|-----------|
| Skill加载 | 自动 | ~1800 |
| S1 | 主会话 | ~200 |
| S2 | 子代理 | ~300 |
| S3 | 主会话 | ~150 |
| S4 | 子代理/跳过 | ~500/0 |
| S5 | 主会话 | ~200 |
| **合计** | | **{总计}** |

---

## 效率优化建议（S5.6）

> 根据审计对象类型提供差异化优化建议。

### 对象类型判定

| 对象类型 | 触发条件 | 适用优化 |
|----------|---------|---------|
| Skill | SKILL.md存在 | 模型调用+Token+多线程 |
| Agent | rules.md或identity-config.md存在 | Token消耗 |
| 工作流 | 多Agent配置或workflow目录存在 | 多线程执行 |
| 财务审计 | G1=财务合规审计 | 合规性检查+风险评级 |

### 1. 模型调用优化（Skill适用）
### 2. Token消耗优化（Skill+Agent适用）
### 3. 多线程执行优化（Skill+工作流适用）

---

## 财务合规审计专项（增强层L）

> 当G1=财务合规审计时填写，否则跳过。

### L1 审计范围
### L2 审计目标
### L3 重要性阈值
### L4 适用标准
### L5 发现列表

| # | 类别 | 问题 | 风险评级 | 纠正措施 |
|---|------|------|---------|---------|
| FC-1 | 财务准确性/合规性违规/控制缺陷/运营低效 | ... | High/Medium/Low | P0/P1/P2 |

---

**审计完成**：{timestamp} | auditor v6.2 | 增强层：{A-L使用列表}